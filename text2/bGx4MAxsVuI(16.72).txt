previous segment JOHN GUTTAG In the last lectur segment we produc one rather uninterest plot We start this one by produc two uninterest plot Let go through the code that produc them becaus the code is at least INAUDIBLE a bit interest First line of code it the same line we start the last program with Pylab is a notion of current figur Execut the statement pylab figur some argument let say n set the current figur to the figur number n Subsequ execut pylab command implicit refer to that figur until anoth pylab figur command is execut Therefor the next line of code pylab plot will draw some point and