So we see the code here Unsurpris we start by import PyLab It not the first time we use import in this class but to remind you import load in a previous defin modul We can then use dot notat to access class function and method to find in that modul Now let run the code and then talk about whi it doe what it did Well I sure you all pleas to see that what the code did wa produc a plot The bar at the top contain the name of the window in this case Figur The figur creat by the first line of code follow the import here it is pylab figur The middl section of the window contain the plot generat by the