the second object And so all the sort routin realli need is for this to be implement on the object in the list and then the sort method will take it from there OK so we onli have to implement the le than special method So if you want to know what all the special method are that you can implement and tap into all sort of great behavior that Python ha built in look at section the Python Languag Refer manual So if we go back and look at the code we can see that we have in fact definin that special le than method for person object What it doe is that it check to see look if the two last name are the same so we pre comput the last name so they avail for each person So we can just quick check to see if they have the same last name