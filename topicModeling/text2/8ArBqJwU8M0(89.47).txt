So when we use the name of a class in this case student use it like a procedur call in this case by pass an argument the string Tim Beaver what Python doe is it creat a new object of type Student and then call init method for that object with this argument So let see if we can figur out what happen So we have an object of type Student and we look for it init method So Student itself defin no new method So we look at the super class of Student which is MITPerson and we see that it doe in fact defin an init method So Python will call this method The first argument for ani method is the object that be manipul in this case the newli creat object of type Student And the second argument is the string Tim Beaver