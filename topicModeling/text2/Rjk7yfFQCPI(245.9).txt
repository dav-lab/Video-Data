time not in term of actual elaps time but in term of the number of basic step that the comput execut We come back to in a second but we go to start with think about how do we avoid and And for that we use what call a random access machin model We go to use that a our model of comput What doe that say It say the step are execut sequenti We not run in parallel And the second assumpt that model make is that a step is an oper that take a constant amount of time And for that we go to assum assign comparison