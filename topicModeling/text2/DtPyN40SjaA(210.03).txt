For exampl PyLab plus plot or PyLab plus matrix By the way this love pictur at the bottom of the slide wa drawn use PyLab Let start with a simpl exampl that us pylab plot to produc well a plot So we see the code here Unsurpris we start by import PyLab It not the first time we use import in this class but to remind you import load in a previous defin modul We can then use dot notat to access class function and method to find in that modul Now let run the code and then talk about whi it doe what it did Well I sure you all pleas to see that what the code did